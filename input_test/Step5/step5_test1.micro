prog step4
begin

	int a, b, i, p;

	func void main()
	begin

		i = 0;
		a = 0;
		b = 0;

		while(i != 10)
			read (p);
			
			if (p > 10) 
				a = a + 1;
			else
      			b = b + 1;
			endif
			i = i+1;
		endwhile

		write (a);
		write (b);
	end
end

